ScreenCtrl.$inject = ['$rootScope', '$scope', 'IHttp', 'toastr'];

module.exports = ScreenCtrl;

function ScreenCtrl($rootScope, $scope, IHttp, toastr) {
    console.log('screenPort:', screenPort);
}
